# Copy to ~/.dbt/profiles.yml for local development outside Docker
ons_pipeline:
  target: dev
  outputs:
    dev:
      type:     postgres
      host:     localhost
      port:     5432
      user:     airflow
      password: airflow
      dbname:   ons_pipeline
      schema:   public
      threads:  2
